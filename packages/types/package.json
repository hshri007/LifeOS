{
  "name": "@lifeos/types",
  "version": "0.1.0",
  "private": true,
  "description": "Shared domain types & schemas for LifeOS (source of truth mirrors spec section 8.4 core data model)",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit -p ."
  },
  "dependencies": {
    "zod": "^3.23.8"
  }
}